import type { AgentContext } from '../agentContext';

export async function runCalendar(ctx: AgentContext): Promise<void> {
  // Placeholder for calendar agent logic
  // Will be implemented when calendar webhook processing is needed
}
